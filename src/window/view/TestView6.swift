import Foundation

class TestView6:AnimatableView {
    let target:CGPoint = CGPoint(100,60)
    var circ:EllipseGraphic!
    override func resolveSkin() {
        super.resolveSkin()
        listAnimTest()
    }
    func listAnimTest(){
        //create a container with a mask 200x200
        
        //create a container with 3 rects insider 
    }
}
